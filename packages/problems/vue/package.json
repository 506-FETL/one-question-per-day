{
  "name": "problem-vue",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "vite --open",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "preview": "vite preview",
    "build": "vite build",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "dependencies": {
    "@one-question-per-day/shared": "workspace:*"
  }
}
